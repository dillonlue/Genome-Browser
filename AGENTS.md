# Agents Guide

- Before acting, read all files in `commands/` so you know available commands (e.g., `#git-save-all` -> `commands/git-save-all.md`).
- When told to use a command like `#git-save-all`, open the corresponding spec in `commands/` and follow its steps exactly.
- If you edit `project_journal/main.tex`, run `project_journal/compile.sh` before finishing so the PDF stays in sync.
