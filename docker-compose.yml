version: "3.9"

services:
  research:
    build: .
    platform: linux/amd64
    container_name: shapley_masking
    tty: true
    stdin_open: true
    volumes:
      - ./:/repo
      - ${HOME}/.codex:/root/.codex
      - ${HOME}/.ssh:/root/.ssh
      - ${HOME}/.gitconfig:/root/.gitconfig
